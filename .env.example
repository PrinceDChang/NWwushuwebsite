# Copy to .env for local development. Do not commit .env.

# Resend (contact + trial emails)
RESEND_API_KEY=re_xxxxxxxx
OWNER_EMAIL=northwestwushu.2008@gmail.com
FROM_EMAIL=Northwest Wushu <onboarding@resend.dev>

# Comma-separated browser origins allowed to call /api (Vite dev + Render URL + custom domain)
ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# Optional local override (production uses PORT from Render)
PORT=3000
